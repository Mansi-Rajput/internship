add = lambda x, y: x + y
print(add(2, 4)) 

# print the lambda function to add two numbers
add = lambda x, y: x + y
# Use the lambda function to add two numbers
result = add(15, 10)
# Print the result
print("The sum of 15 and 10 is:", result)
